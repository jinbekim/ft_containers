#ifndef MAP_HPP
#define MAP_HPP

#include "util.h"
namespace ft
{
class map
{
	public:
		map(/* args*/);
		map(const map &ref);
		~map();
		map& operator=(const map &ref);
	private:
		/* data */
};

}

#endif
